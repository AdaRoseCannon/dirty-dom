{
  "name": "dirty-dom-lib",
  "version": "1.0.0",
  "description": "Don't use this....",
  "main": "dirty-filthy-script.js",
  "dependencies": {
    "marked": "^0.3.5"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.3.13",
    "babelify": "^7.2.0",
    "browserify": "^12.0.1"
  },
  "scripts": {
    "test": "echo \"It doesn't even have any tests!!\"",
    "build": "mkdir build; browserify ./dirty-filthy-script.js -t [ babelify --presets [ es2015] ]] -o ./build/dirty-dom-lib.js"
  },
  "author": "",
  "license": "ISC"
}
